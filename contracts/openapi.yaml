openapi: 3.1.0
info:
  title: 智慧云脑诊疗平台 API
  version: 0.1.0
  description: |
    Stage 0 契约根文件。当前只冻结公共响应、安全方案和分页结构，
    不包含未经任务卡批准的医疗业务接口。
servers:
  - url: http://localhost:8080
    description: 本地后端
  - url: http://localhost:4010
    description: Prism API Mock Server
paths:
  /api/auth/login:
    post:
      tags:
        - 认证
      summary: 用户登录
      description: |
        使用用户名和密码进行身份验证，成功后返回 JWT Access Token。

        安全机制：
        - 密码使用 BCrypt（cost=12）哈希存储和校验
        - 连续 5 次失败锁定账号 15 分钟
        - 按 IP + 用户名维度限流，5 分钟内最多 10 次失败
        - 初始管理员首次登录必须修改密码

        JWT Claims 结构：
        - sub: 用户 ID
        - roles: 角色列表
        - tokenVersion: Token 版本号
        - iat: 签发时间
        - exp: 过期时间（+2 小时）
        - jti: 唯一标识
      operationId: login
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: './schemas/auth.yaml#/LoginRequest'
            examples:
              正常登录:
                value:
                  username: admin
                  password: "Admin@2026"
      responses:
        '200':
          description: 登录成功
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: './schemas/auth.yaml#/LoginResponse'
              examples:
                成功响应:
                  value:
                    code: SUCCESS
                    message: 操作成功
                    data:
                      accessToken: "eyJhbGciOiJIUzI1NiJ9..."
                      tokenType: Bearer
                      userId: 1
                      username: admin
                      roles:
                        - ADMIN
                      mustChangePassword: true
                      expiresIn: 7200
                    traceId: "abc-123-def"
                强制改密提示:
                  value:
                    code: SUCCESS
                    message: 操作成功
                    data:
                      accessToken: "eyJhbGciOiJIUzI1NiJ9..."
                      tokenType: Bearer
                      userId: 1
                      username: admin
                      roles:
                        - ADMIN
                      mustChangePassword: true
                      expiresIn: 7200
                    traceId: "abc-123-def"
        '400':
          description: 参数校验失败
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
              examples:
                缺少字段:
                  value:
                    code: VALIDATION_FAILED
                    message: "用户名不能为空"
                    data: null
                    traceId: "abc-123-def"
                密码太短:
                  value:
                    code: VALIDATION_FAILED
                    message: "密码长度必须在 8-64 之间"
                    data: null
                    traceId: "abc-123-def"
        '401':
          description: 认证失败
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
              examples:
                错误凭证:
                  value:
                    code: AUTH_INVALID_CREDENTIALS
                    message: "用户名或密码错误"
                    data: null
                    traceId: "abc-123-def"
                账号锁定:
                  value:
                    code: AUTH_ACCOUNT_LOCKED
                    message: "账号已锁定，10 分钟后重试"
                    data: null
                    traceId: "abc-123-def"
                账号禁用:
                  value:
                    code: AUTH_ACCOUNT_DISABLED
                    message: "账号已禁用"
                    data: null
                    traceId: "abc-123-def"
        '429':
          description: 登录限流
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
              examples:
                限流:
                  value:
                    code: AUTH_LOGIN_RATE_LIMITED
                    message: "登录尝试过于频繁"
                    data: null
                    traceId: "abc-123-def"

  /api/auth/logout:
    post:
      tags:
        - 认证
      summary: 退出登录
      description: |
        清除当前用户的认证状态。

        后端处理：
        - 递增当前用户的 tokenVersion，使所有旧 Token 失效
        - 前端需要清除本地存储的 Token 并跳转到登录页

        认证要求：Bearer Token
      operationId: logout
      security:
        - bearerAuth: []
      responses:
        '200':
          description: 退出成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
              examples:
                成功:
                  value:
                    code: SUCCESS
                    message: 操作成功
                    data: null
                    traceId: "abc-123-def"
        '401':
          description: 未认证或 Token 已失效
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
              examples:
                Token 失效:
                  value:
                    code: AUTH_TOKEN_REVOKED
                    message: "Token 已失效，请重新登录"
                    data: null
                    traceId: "abc-123-def"
                未登录:
                  value:
                    code: AUTH_TOKEN_REVOKED
                    message: "未登录或 Token 已失效"
                    data: null
                    traceId: "abc-123-def"

  /api/auth/change-password:
    post:
      tags:
        - 认证
      summary: 修改密码
      description: |
        修改当前用户的密码。

        业务规则：
        - 必须提供正确的原密码
        - 新密码不能与原密码相同
        - 修改成功后自动清除 mustChangePassword 标记
        - 递增 tokenVersion 使旧 Token 失效，需重新登录

        认证要求：Bearer Token
      operationId: changePassword
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: './schemas/auth.yaml#/ChangePasswordRequest'
            examples:
              正常修改:
                value:
                  oldPassword: "OldPassword123!"
                  newPassword: "NewPassword123!"
      responses:
        '200':
          description: 修改成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
              examples:
                成功:
                  value:
                    code: SUCCESS
                    message: 操作成功
                    data: null
                    traceId: "abc-123-def"
        '400':
          description: 参数校验失败
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
              examples:
                新旧密码相同:
                  value:
                    code: VALIDATION_FAILED
                    message: "新密码不能与原密码相同"
                    data: null
                    traceId: "abc-123-def"
        '401':
          description: 认证失败或原密码错误
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
              examples:
                原密码错误:
                  value:
                    code: AUTH_INVALID_CREDENTIALS
                    message: "原密码错误"
                    data: null
                    traceId: "abc-123-def"
                未登录:
                  value:
                    code: AUTH_TOKEN_REVOKED
                    message: "未登录或 Token 已失效"
                    data: null
                    traceId: "abc-123-def"

  # ============================================================
  # 科室接口
  # ============================================================
  /api/departments/tree:
    get:
      tags:
        - 科室
      summary: 获取科室树形结构
      operationId: getDepartmentTree
      security:
        - bearerAuth: []
      responses:
        '200':
          description: 科室树
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: './schemas/base_data.yaml#/DepartmentResponse'

  /api/departments:
    get:
      tags:
        - 科室
      summary: 获取科室扁平列表
      operationId: getDepartmentList
      security:
        - bearerAuth: []
      responses:
        '200':
          description: 科室列表
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: './schemas/base_data.yaml#/DepartmentResponse'
    post:
      tags:
        - 科室
      summary: 创建科室
      description: 管理员创建科室
      operationId: createDepartment
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: './schemas/base_data.yaml#/DepartmentCreateRequest'
      responses:
        '200':
          description: 创建成功
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: './schemas/base_data.yaml#/DepartmentResponse'

  /api/departments/{id}:
    get:
      tags:
        - 科室
      summary: 获取科室详情
      operationId: getDepartmentById
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: 科室详情
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: './schemas/base_data.yaml#/DepartmentResponse'
    put:
      tags:
        - 科室
      summary: 更新科室
      description: 管理员更新科室信息
      operationId: updateDepartment
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: './schemas/base_data.yaml#/DepartmentUpdateRequest'
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: './schemas/base_data.yaml#/DepartmentResponse'

  # ============================================================
  # 患者接口
  # ============================================================
  /api/patients/register:
    post:
      tags:
        - 患者
      summary: 患者注册
      description: 患者自助注册建档
      operationId: registerPatient
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: './schemas/base_data.yaml#/PatientRegisterRequest'
      responses:
        '200':
          description: 注册成功
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: './schemas/base_data.yaml#/PatientResponse'

  /api/patients/me:
    get:
      tags:
        - 患者
      summary: 获取当前登录患者信息
      operationId: getCurrentPatient
      security:
        - bearerAuth: []
      responses:
        '200':
          description: 患者信息
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: './schemas/base_data.yaml#/PatientResponse'

  /api/patients/{id}:
    get:
      tags:
        - 患者
      summary: 获取患者详情
      description: 权限校验：患者只能查看自己的信息
      operationId: getPatientById
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: 患者详情
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: './schemas/base_data.yaml#/PatientResponse'
    put:
      tags:
        - 患者
      summary: 更新患者信息
      description: 患者只能更新自己的信息
      operationId: updatePatient
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: './schemas/base_data.yaml#/PatientUpdateRequest'
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: './schemas/base_data.yaml#/PatientResponse'

  /api/patients/{id}/profile:
    get:
      tags:
        - 患者
      summary: 获取患者档案
      operationId: getPatientProfile
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: 患者档案
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: './schemas/base_data.yaml#/PatientProfileResponse'
    put:
      tags:
        - 患者
      summary: 更新患者档案
      operationId: updatePatientProfile
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: './schemas/base_data.yaml#/PatientProfileUpdateRequest'
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: './schemas/base_data.yaml#/PatientProfileResponse'

  /api/patients/search:
    get:
      tags:
        - 患者
      summary: 管理员搜索患者
      description: 按姓名或手机号搜索
      operationId: searchPatients
      security:
        - bearerAuth: []
      parameters:
        - name: name
          in: query
          schema:
            type: string
        - name: phone
          in: query
          schema:
            type: string
      responses:
        '200':
          description: 搜索结果
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: './schemas/base_data.yaml#/PatientResponse'

  # ============================================================
  # 医生接口
  # ============================================================
  /api/doctors:
    get:
      tags:
        - 医生
      summary: 获取医生列表（分页）
      operationId: getDoctorList
      security:
        - bearerAuth: []
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: pageSize
          in: query
          schema:
            type: integer
            default: 20
        - name: name
          in: query
          schema:
            type: string
      responses:
        '200':
          description: 医生列表
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/PageResponse'
    post:
      tags:
        - 医生
      summary: 创建医生
      description: 管理员创建医生账号
      operationId: createDoctor
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: './schemas/base_data.yaml#/DoctorCreateRequest'
      responses:
        '200':
          description: 创建成功
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: './schemas/base_data.yaml#/DoctorResponse'

  /api/doctors/{id}:
    get:
      tags:
        - 医生
      summary: 获取医生详情
      operationId: getDoctorById
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: 医生详情
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: './schemas/base_data.yaml#/DoctorResponse'
    put:
      tags:
        - 医生
      summary: 更新医生信息
      operationId: updateDoctor
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: './schemas/base_data.yaml#/DoctorUpdateRequest'
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: './schemas/base_data.yaml#/DoctorResponse'

  /api/doctors/by-department/{deptId}:
    get:
      tags:
        - 医生
      summary: 按科室查询医生
      operationId: getDoctorsByDepartment
      security:
        - bearerAuth: []
      parameters:
        - name: deptId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: 科室下医生列表
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: './schemas/base_data.yaml#/DoctorResponse'

  # ============================================================
  # 药品接口
  # ============================================================
  /api/drugs:
    get:
      tags:
        - 药品
      summary: 获取药品列表（分页）
      operationId: getDrugList
      security:
        - bearerAuth: []
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: pageSize
          in: query
          schema:
            type: integer
            default: 20
        - name: name
          in: query
          schema:
            type: string
      responses:
        '200':
          description: 药品列表
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/PageResponse'

  /api/drugs/{id}:
    get:
      tags:
        - 药品
      summary: 获取药品详情
      description: 含成分、剂量规则和禁忌
      operationId: getDrugById
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: 药品详情
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: './schemas/base_data.yaml#/DrugResponse'

  /api/drugs/by-code/{code}:
    get:
      tags:
        - 药品
      summary: 按编码查询药品
      operationId: getDrugByCode
      security:
        - bearerAuth: []
      parameters:
        - name: code
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: 药品详情
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: './schemas/base_data.yaml#/DrugResponse'

  /api/drugs/by-category:
    get:
      tags:
        - 药品
      summary: 按分类查询药品
      operationId: getDrugsByCategory
      security:
        - bearerAuth: []
      parameters:
        - name: category
          in: query
          required: true
          schema:
            type: string
            enum: [WESTERN, CHINESE]
      responses:
        '200':
          description: 药品列表
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: './schemas/base_data.yaml#/DrugResponse'

  /api/drugs/interaction:
    get:
      tags:
        - 药品
      summary: 查询药品相互作用
      operationId: getDrugInteraction
      security:
        - bearerAuth: []
      parameters:
        - name: drugACode
          in: query
          schema:
            type: string
        - name: drugBCode
          in: query
          schema:
            type: string
        - name: drugCode
          in: query
          schema:
            type: string
      responses:
        '200':
          description: 相互作用列表
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: './schemas/base_data.yaml#/DrugInteractionResponse'

  /api/drugs/contraindications/{code}:
    get:
      tags:
        - 药品
      summary: 查询药品禁忌
      operationId: getDrugContraindications
      security:
        - bearerAuth: []
      parameters:
        - name: code
          in: path
          required: true
          schema:
            type: string
        - name: conditionType
          in: query
          schema:
            type: string
            enum: [ALLERGY, DISEASE, PREGNANCY, AGE]
      responses:
        '200':
          description: 禁忌列表
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          type: object
                          properties:
                            conditionType:
                              type: string
                            conditionValue:
                              type: string
                            description:
                              type: string
  # ============================================================
  # 排班接口
  # ============================================================
  /api/schedules:
    post:
      tags:
        - 排班
      summary: 创建排班（管理员）
      operationId: createSchedule
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: './schemas/schedule_appointment.yaml#/ScheduleCreateRequest'
      responses:
        '200':
          description: 创建成功
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: './schemas/schedule_appointment.yaml#/ScheduleResponse'
        '400':
          description: 参数校验失败或业务冲突
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
  /api/schedules/{id}:
    get:
      tags:
        - 排班
      summary: 获取排班详情
      operationId: getScheduleById
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: 排班详情
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: './schemas/schedule_appointment.yaml#/ScheduleResponse'
    put:
      tags:
        - 排班
      summary: 修改排班（管理员）
      operationId: updateSchedule
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: './schemas/schedule_appointment.yaml#/ScheduleUpdateRequest'
      responses:
        '200':
          description: 修改成功
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: './schemas/schedule_appointment.yaml#/ScheduleResponse'
  /api/schedules/{id}/cancel:
    post:
      tags:
        - 排班
      summary: 取消排班（管理员）
      description: |
        取消排班与取消其 BOOKED、CHECKED_IN 挂号在同一事务完成。
        存在 IN_PROGRESS、WAITING_EXAM 或 COMPLETED 挂号时禁止取消。
      operationId: cancelSchedule
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: './schemas/schedule_appointment.yaml#/ScheduleCancelRequest'
      responses:
        '200':
          description: 取消成功
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: './schemas/schedule_appointment.yaml#/ScheduleResponse'
  /api/schedules/doctor-list/{doctorId}:
    get:
      tags:
        - 排班
      summary: 按医生查询排班
      operationId: getSchedulesByDoctor
      security:
        - bearerAuth: []
      parameters:
        - name: doctorId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: 排班列表
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: './schemas/schedule_appointment.yaml#/ScheduleResponse'
  /api/schedules/dept-list/{departmentId}:
    get:
      tags:
        - 排班
      summary: 按科室查询排班
      operationId: getSchedulesByDepartment
      security:
        - bearerAuth: []
      parameters:
        - name: departmentId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: 排班列表
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: './schemas/schedule_appointment.yaml#/ScheduleResponse'
  /api/schedules/available:
    get:
      tags:
        - 排班
      summary: 查询可预约排班（患者端）
      operationId: getAvailableSchedules
      security:
        - bearerAuth: []
      parameters:
        - name: departmentId
          in: query
          schema:
            type: integer
            format: int64
        - name: date
          in: query
          schema:
            type: string
            format: date-time
      responses:
        '200':
          description: 可预约排班列表
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: './schemas/schedule_appointment.yaml#/ScheduleResponse'
  # ============================================================
  # 挂号接口
  # ============================================================
  /api/appointments:
    post:
      tags:
        - 挂号
      summary: 创建挂号
      description: |
        并发控制：使用数据库条件更新防止超卖。
        事务保证：号源扣减与挂号记录创建在同一事务。
        约束：
        - 同一患者同一排班不能重复挂号
        - 过期或取消排班不能继续挂号
      operationId: createAppointment
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: './schemas/schedule_appointment.yaml#/AppointmentCreateRequest'
      responses:
        '200':
          description: 挂号成功
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: './schemas/schedule_appointment.yaml#/AppointmentResponse'
        '400':
          description: 参数校验失败或业务冲突（号源已满、重复挂号等）
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
    get:
      tags:
        - 挂号
      summary: 查询全部挂号（管理员）
      operationId: getAllAppointments
      security:
        - bearerAuth: []
      responses:
        '200':
          description: 挂号列表
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: './schemas/schedule_appointment.yaml#/AppointmentResponse'
  /api/appointments/{id}:
    get:
      tags:
        - 挂号
      summary: 获取挂号详情
      operationId: getAppointmentById
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: 挂号详情
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: './schemas/schedule_appointment.yaml#/AppointmentResponse'
  /api/appointments/{id}/cancel:
    post:
      tags:
        - 挂号
      summary: 取消挂号
      description: |
        事务保证：取消挂号与恢复号源在同一事务。
        约束：接诊开始后（IN_PROGRESS、WAITING_EXAM）不得取消。
      operationId: cancelAppointment
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: './schemas/schedule_appointment.yaml#/AppointmentCancelRequest'
      responses:
        '200':
          description: 取消成功
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: './schemas/schedule_appointment.yaml#/AppointmentResponse'
  /api/appointments/patient-list/{patientId}:
    get:
      tags:
        - 挂号
      summary: 查询患者挂号列表
      operationId: getAppointmentsByPatient
      security:
        - bearerAuth: []
      parameters:
        - name: patientId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: 患者挂号列表
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: './schemas/schedule_appointment.yaml#/AppointmentResponse'
  /api/appointments/doctor-list/{doctorId}:
    get:
      tags:
        - 挂号
      summary: 查询医生挂号列表
      operationId: getAppointmentsByDoctor
      security:
        - bearerAuth: []
      parameters:
        - name: doctorId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: 医生挂号列表
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: './schemas/schedule_appointment.yaml#/AppointmentResponse'
  /api/appointments/doctor/{doctorId}/pending:
    get:
      tags:
        - 挂号
      summary: 查询医生待诊队列
      operationId: getDoctorPendingAppointments
      security:
        - bearerAuth: []
      parameters:
        - name: doctorId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: 待诊队列
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: './schemas/schedule_appointment.yaml#/AppointmentResponse'
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    ApiResponse:
      $ref: ./schemas/common.yaml#/ApiResponse
    PageResponse:
      $ref: ./schemas/common.yaml#/PageResponse
    ErrorCode:
      $ref: ./schemas/common.yaml#/ErrorCode
