# 任务二：智能分诊能力 — 交付说明

> 任务编号：STAGE-AI-2
> 目标分支：`feature/ai-triage`
> 契约版本：AI 协作文档 v2.5
> 依据：[AI能力集成开发计划.md](file:///d:/shixun/AI能力集成开发计划.md#L91-L174)、[32_AI能力契约规范.md](file:///d:/shixun/yunnao/智慧云脑诊疗平台_AI协作文档_v2/contracts/32_AI能力契约规范.md)、[13_AI能力集成AI任务书.md](file:///d:/shixun/yunnao/智慧云脑诊疗平台_AI协作文档_v2/roles/13_AI能力集成AI任务书.md)、[41_质量测试与完成定义.md](file:///d:/shixun/yunnao/智慧云脑诊疗平台_AI协作文档_v2/workflow/41_质量测试与完成定义.md)

---

## 1. 实现的 AI 能力

智能分诊：将 [AITriageService](file:///d:/shixun/yunnao/backend/src/main/java/com/neusoft/cloudbrain/ai/api/AITriageService.java) 从关键词 Mock 升级为 Provider + Prompt + Schema 的真实能力实现，输出科室编码、优先级、症状关键词、理由和安全提示。

---

## 2. 输入输出 Schema

### 输入（[TriageAIRequest](file:///d:/shixun/yunnao/backend/src/main/java/com/neusoft/cloudbrain/ai/dto/TriageAIRequest.java)）

| 字段 | 类型 | 说明 |
|---|---|---|
| ageRange | String | 年龄区间 |
| gender | String | 性别 |
| chiefComplaint | String | 主诉 |
| duration | String | 症状持续时间 |
| supplement | String | 补充描述 |

不包含患者 ID、姓名、手机号等隐私信息（最小化原则）。

### 输出（[TriageAIResult](file:///d:/shixun/yunnao/backend/src/main/java/com/neusoft/cloudbrain/ai/dto/TriageAIResult.java)）

| 字段 | 类型 | 受控规则 |
|---|---|---|
| departmentCode | String | 枚举受控：DEPT_INTERNAL / DEPT_CARDIOLOGY / DEPT_NEUROLOGY / DEPT_SURGERY / DEPT_GENERAL_SURGERY / DEPT_PEDIATRICS / DEPT_EMERGENCY |
| priority | String | 枚举受控：EMERGENCY / HIGH / MEDIUM / LOW |
| symptomKeywords | List\<String\> | 识别到的关键症状词 |
| reason | String | 推荐理由 |
| safetyNotice | String | 安全提示（含"仅供辅助参考"） |
| emergencySuggested | boolean | 是否建议走急诊通道 |

---

## 3. Prompt 文件

[triage_v1.txt](file:///d:/shixun/yunnao/backend/src/main/resources/prompts/triage_v1.txt)

本次更新内容（对照 [32_AI能力契约规范.md 第6节](file:///d:/shixun/yunnao/智慧云脑诊疗平台_AI协作文档_v2/contracts/32_AI能力契约规范.md)）：

- 明确任务边界（仅分诊建议，不进行确定性诊断）
- 指定输出 JSON Schema（含字段说明和类型）
- 禁止输出医生 ID、医生姓名或任何医生标识信息
- 禁止作出确定性医疗诊断
- 禁止编造患者未提及的病史、检查结果或过敏史
- departmentCode 受控枚举明确列出
- 缺失信息时在 reason 中说明，不得编造
- 包含"仅供辅助参考"安全声明

---

## 4. Provider 变化

### AITriageServiceImpl 改造

[AITriageServiceImpl](file:///d:/shixun/yunnao/backend/src/main/java/com/neusoft/cloudbrain/ai/application/AITriageServiceImpl.java) 变更：

1. **新增 departmentCode 受控校验**：`ALLOWED_DEPARTMENTS` 集合，来自 [V010__base_data.sql](file:///d:/shixun/yunnao/backend/src/main/resources/db/migration/V010__base_data.sql) 基础数据（7 个科室编码）
2. **错误码修正**：
   - 超时/Provider 异常 → `AI_TRIAGE_FAILED`（HTTP 504）
   - 非法 JSON / Schema 校验失败 → `AI_INVALID_RESPONSE`（HTTP 500）
   - 对照任务卡异常流程规范

### MockAIProvider 调整

[MockAIProvider](file:///d:/shixun/yunnao/backend/src/main/java/com/neusoft/cloudbrain/ai/provider/MockAIProvider.java) 变更：

- 空结果场景：从空字符串 `departmentCode:""` 改为受控默认值 `DEPT_INTERNAL` + `LOW`，使空结果仍通过 Schema 校验

---

## 5. Mock 场景

7 场景全覆盖（对照 [32_AI能力契约规范.md 第4节](file:///d:/shixun/yunnao/智慧云脑诊疗平台_AI协作文档_v2/contracts/32_AI能力契约规范.md)）：

| 场景 | 触发关键词 | 行为 |
|---|---|---|
| 正常结果 | 默认 | 基于医疗关键词路由到对应科室 |
| 高风险结果 | MOCK_HIGH_RISK 或胸痛关键词 | 胸痛→DEPT_CARDIOLOGY + HIGH + emergencySuggested=true |
| 空结果 | MOCK_EMPTY | DEPT_INTERNAL + LOW + 空关键词 + "信息不足" |
| 超时 | MOCK_TIMEOUT | 抛 AIProviderException(retryable=true) |
| 非法 JSON | MOCK_INVALID_JSON | 返回非 JSON 文本 |
| 不存在科室 | MOCK_NOT_EXIST_DEPT | 返回 DEPT_NOT_EXIST_999（校验失败） |
| Provider 异常 | MOCK_PROVIDER_ERROR | 抛 AIProviderException(retryable=false, 500) |

---

## 6. 超时和降级策略

```text
超时 → 重试 1 次 → 仍失败 → AIInvocation(FAILED) → 抛 AI_TRIAGE_FAILED (HTTP 504) → 业务进手动流程
非法 JSON → AI_INVALID_RESPONSE → 不无限修复 → 降级
不存在科室 → departmentCode 校验失败 → AI_INVALID_RESPONSE → 业务侧拒绝
```

- 重试仅对超时/5xx，非法 JSON 不重试
- 不无限修复模型输出
- 1 次业务调用 = 1 条 AIInvocation + N 条 AIInvocationAttempt

---

## 7. 测试结果

### AITriageServiceImplTest — 17 个用例

| 分组 | 用例数 | 说明 |
|---|---:|---|
| 5 类主诉路径 | 5 | 胸痛→心血管内科 HIGH / 头痛→神经内科 MEDIUM / 发热→内科 MEDIUM / 腹痛→内科 MEDIUM / 外伤→普通外科 MEDIUM |
| 7 Mock 场景 | 7 | 正常 / 高风险 / 空 / 超时 / 非法JSON / 不存在科室 / Provider异常 |
| Schema 校验 | 3 | priority 枚举非法 / departmentCode 枚举非法 / 缺少必填字段 |
| 安全性验证 | 2 | 安全提示含"仅供辅助参考" / markdown fence 解析 |

### MockAIProviderTest — 16 个用例

含七场景分流 + 五能力响应格式 + 安全声明 + mock 标识。

### 全量 AI 测试 — 74 个用例全部通过

```
Tests run: 74, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

### TriageServiceTest（业务模块）— 9 个用例全部通过

```
Tests run: 9, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

无回归。

---

## 8. 对后端业务模块的调用说明

- AI 分诊不直接对外暴露 HTTP，由 triage 业务模块编排调用
- 调用入口：`AITriageService.analyze(TriageAIRequest)`
- 输入不含患者隐私 ID（最小化原则）
- 异常时业务模块捕获 `BusinessException`，按错误码降级：
  - `AI_TRIAGE_FAILED` → 转手动选择科室
  - `AI_INVALID_RESPONSE` → 转手动选择科室

---

## 9. 潜在安全风险

- Prompt 中明确禁止输出医生 ID 和确定性诊断，但真实模型可能不遵守 → 由 JsonSchemaParser 做枚举受控兜底
- 输入已做脱敏（不含患者隐私 ID），但需业务模块确保传入前完成脱敏
- 日志和 AIInvocation 中不保存 API Key（由 AIInvocationRecorder 保证）

---

## 10. 未解决问题

- 受控科室编码目前硬编码在 AITriageServiceImpl 中，若后续科室新增需同步修改 → 后续可考虑从数据库或配置读取
- JSON Schema 校验仍为手写降级方案，是否引入 `com.networknt:json-schema-validator` 待后端角色确认

---

## 交付物对比

| 任务卡要求 | 交付状态 |
|---|---|
| prompts/triage-system.txt | ✅ [triage_v1.txt](file:///d:/shixun/yunnao/backend/src/main/resources/prompts/triage_v1.txt)（按 PromptManager 命名规范） |
| 分诊 Schema 校验：priority 受控 | ✅ `ALLOWED_PRIORITIES = {EMERGENCY, HIGH, MEDIUM, LOW}` |
| 分诊 Schema 校验：departmentCode 受控 | ✅ `ALLOWED_DEPARTMENTS`（7 个科室编码） |
| AITriageServiceImpl 改造 | ✅ Provider 调用 + Schema 校验 + 调用记录 |
| MockAIProvider 7 场景 | ✅ 正常/高风险/空/超时/非法JSON/不存在科室/异常 |

## 修改边界遵守

```text
允许修改：ai/**、prompts/triage_v1.txt ✅
禁止修改：triage 业务模块 ✅（未修改）、其他业务模块 ✅、pom.xml ✅、contracts/** ✅
```

修改文件清单：
1. `backend/src/main/resources/prompts/triage_v1.txt` — Prompt 增强
2. `backend/src/main/java/.../ai/application/AITriageServiceImpl.java` — departmentCode 校验 + 错误码修正
3. `backend/src/main/java/.../ai/provider/MockAIProvider.java` — 空结果响应修正
4. `backend/src/test/java/.../ai/application/AITriageServiceImplTest.java` — 测试重写
5. `backend/src/test/java/.../ai/provider/MockAIProviderTest.java` — 空结果断言修正
